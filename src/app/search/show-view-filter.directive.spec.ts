import { ShowViewFilterDirective } from './show-view-filter.directive';

describe('ShowViewFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowViewFilterDirective();
    expect(directive).toBeTruthy();
  });
});
